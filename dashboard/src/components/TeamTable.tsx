import type { RescueTeam } from "../services/api";
export default function TeamTable({ teams }: { teams: RescueTeam[] }) {
  return <div className="overflow-hidden rounded-xl border border-gray-200 bg-white shadow-sm"><div className="overflow-x-auto"><table className="w-full min-w-[700px] text-left"><thead className="border-b border-gray-200 bg-gray-50"><tr>{["Team", "Callsign", "Location", "Status"].map(label => <th key={label} className="px-5 py-4 text-xs font-semibold uppercase tracking-wide text-gray-500">{label}</th>)}</tr></thead><tbody className="divide-y divide-gray-100">
    {teams.map(team => <tr key={team.teamId} className="hover:bg-red-50/40"><td className="px-5 py-4"><p className="text-sm font-semibold text-gray-800">{team.teamName}</p><p className="text-xs text-gray-400">{team.teamId}</p></td><td className="px-5 py-4 text-sm text-gray-600">{team.callsign}</td><td className="px-5 py-4 text-sm text-gray-600">{team.district}, {team.province}</td><td className="px-5 py-4"><span className="rounded-full bg-green-50 px-2.5 py-1 text-xs font-medium text-green-600">{team.status}</span></td></tr>)}
  </tbody></table></div>{teams.length === 0 && <p className="p-6 text-sm text-gray-500">No official rescue teams found.</p>}</div>;
}
